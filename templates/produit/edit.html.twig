{% extends 'admin/index.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <h1>Edit Produit</h1>




<style>
.button-group {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
}

.button-group form {
    margin: 0; /* Removes any default margin */
    display: inline-block; /* Helps the form fit into the flex layout */
}
</style>



    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

<div class="button-group">
    <a href="{{ path('app_produit_index') }}" class="btn btn-dark">back to list</a>

    {{ include('produit/_delete_form.html.twig') }}
</div>

{% endblock %}
